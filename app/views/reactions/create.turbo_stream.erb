<turbo-stream action="replace" target="reactions_<%= dom_id(@reaction.reactable) %>">
  <template>
<%= render partial: "reactions/reactions", formats: [:html], locals: { reactionable: @reaction.reactable } %>

  </template>
</turbo-stream>
