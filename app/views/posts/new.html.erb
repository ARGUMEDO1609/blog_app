<%= turbo_frame_tag "modal" do %>
  <div class="fixed top-0 right-0 h-full w-full sm:w-1/3 bg-white shadow-xl border-l border-gray-200 z-50 overflow-y-auto p-6 animate-slide-in">
    <% content_for :title, "New post" %>

    <div class="space-y-6">
      <h1 class="font-bold text-3xl flex items-center gap-2">
        <span>📝</span> New post
      </h1>

      <%= render "form", post: @post %>

      <div class="flex gap-2">
        <%= link_to "Cancel", "#",
          data: { controller: "modal", action: "modal#close" },
          class: "cancel-button w-full sm:w-auto rounded-md px-4 py-2 bg-gray-100 hover:bg-gray-200 text-gray-800 font-medium" %>
      </div>
    </div>
  </div>
<% end %>
