<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Blog App" %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <!-- Navegación -->
    <nav class="p-4 bg-gray-800 text-white flex justify-between items-center">
      <div>
        <%= link_to "BLOG DE PREGUNTAS", root_path, class: "font-bold text-xl" %>
      </div>

      <div class="flex items-center space-x-4">
        <% if user_signed_in? %>
          <!-- Suscripción en vivo al contador -->
          <%= turbo_stream_from "notifications_count_#{current_user.id}" %>

          <!-- Enlace con campana y contador -->
          <%= link_to notifications_path, class: "relative flex items-center" do %>
            🛎
            <span id="notifications_count" class="absolute -top-2 -right-2 bg-red-500 text-white rounded-full px-2 text-xs">
              <%= render "notifications/count", count: current_user.notifications.where(read: false).count %>
            </span>
          <% end %>

          <%= button_to "Cerrar sesión", destroy_user_session_path,
                        method: :delete,
                        class: "bg-red-500 text-white px-4 py-2 rounded" %>
        <% else %>
          <%= link_to "Iniciar sesión", new_user_session_path, class: "ml-4" %>
          <%= link_to "Registrarse", new_user_registration_path, class: "ml-4" %>
        <% end %>
      </div>
    </nav>

    <!-- Mensajes flash -->
    <% if notice %>
      <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative m-4">
        <strong class="font-bold">¡Éxito!</strong>
        <span class="block sm:inline"><%= notice %></span>
      </div>
    <% end %>

    <% if alert %>
      <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative m-4">
        <strong class="font-bold">¡Error!</strong>
        <span class="block sm:inline"><%= alert %></span>
      </div>
    <% end %>

    <!-- Contenido principal -->
    <main class="container mx-auto mt-8 px-5">
      <%= yield %>
    </main>
  </body>
</html>
