<div class="max-w-3xl mx-auto mt-10 bg-white p-6 rounded shadow">
  <h1 class="text-2xl font-bold mb-4">Notificaciones</h1>

  <% if @notifications.any? %>
    <% @notifications.each do |notification| %>
      <div class="mb-4 p-4 border rounded <%= notification.read? ? 'bg-gray-100' : 'bg-blue-100' %>">
        <p class="text-gray-800"><%= notification.message %></p>
        <% unless notification.read? %>
          <%= button_to "Marcar como leída", notification_path(notification), method: :patch, class: "text-sm text-blue-600" %>
        <% end %>
      </div>
    <% end %>
  <% else %>
    <p class="text-gray-500">No tienes notificaciones.</p>
  <% end %>

  <%= link_to "Volver", root_path, class: "inline-block mt-4 bg-gray-500 text-white px-4 py-2 rounded" %>
</div>
